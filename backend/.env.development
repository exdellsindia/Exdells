# Local development environment (do NOT commit secrets)
# If you have Postgres locally, set DATABASE_URL to use it; otherwise leave unset to use sqlite file fallback.
# Example Postgres (uncomment to use):
# DATABASE_URL=postgres://exdells:exdellspass@localhost:5432/exdells

# If left empty, backend will use a sqlite file at backend_dev.sqlite
JWT_SECRET=dev_replace_with_secret
PORT=4000
CORS_ORIGIN=http://localhost:5173
NODE_ENV=development
