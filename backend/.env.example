# Copy this file to .env and update values before running in production
# Database connection (Postgres)
DATABASE_URL=postgres://exdells:exdellspass@db:5432/exdells
# JWT signing secret (use a long random string)
JWT_SECRET=replace_with_a_strong_secret
# App port
PORT=4000

# Cloudinary server-side credentials (required for server-side file uploads)
# Get these from Cloudinary dashboard
CLOUDINARY_CLOUD_NAME=dqbyh7ste
CLOUDINARY_API_KEY=589443142596577
CLOUDINARY_API_SECRET=your_api_secret_here

# SMTP settings for email notifications (add your SMTP credentials)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASS=your_smtp_password
EMAIL_FROM="Exdells Website" <no-reply@example.com>
EMAIL_TO=leads@example.com

# Optional: CORS origin (frontend URL)
#CORS_ORIGIN=http://localhost:5173
# Set NODE_ENV=production for production deployments
#NODE_ENV=production
