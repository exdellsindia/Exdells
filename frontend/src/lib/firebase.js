// Firebase support removed — attachments are handled server-side via Cloudinary.
// Keep this file as a stub to avoid build issues if a stray import exists.
export function uploadFileToFirebase() {
  throw new Error('Firebase support removed. Use server-side uploads (Cloudinary) instead.')
}
