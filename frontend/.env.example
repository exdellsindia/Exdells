# Copy this to .env.development in frontend and fill values for client-side uploads
# Firebase config for client-side storage (create a Firebase project + enable Storage)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
# Note: bucket name is commonly <project-id>.appspot.com (e.g. exdells-solar.appspot.com). If you see CORS or 404/403 errors, try the .appspot.com variant.
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Optional: API base for backend (if using your own backend instead of serverless)
# VITE_API_BASE_URL=https://api.exdells.com
