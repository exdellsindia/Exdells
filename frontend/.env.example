# Copy this to .env.development in frontend and fill values as needed
# Optional: API base for backend (if using your own backend instead of serverless)
# VITE_API_BASE_URL=https://api.exdells.com

# Attachments are uploaded to the backend and handled server-side (Cloudinary).
# No client-side storage config is required; configure CLOUDINARY_* in backend for file uploads.
